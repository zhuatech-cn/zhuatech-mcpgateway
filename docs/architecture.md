# MCP Gateway 架构

版权所有 © 2026 上海如静知华信息科技有限公司。

控制面由 Vue 管理端、Spring Boot 服务注册与策略 API、MySQL 台账组成；运行面负责身份上下文、Schema 校验、策略决策、凭证代理、调用转发和审计。社区版演示控制面并提供确定性的访问判定服务。

生产部署应将运行面做成无状态高可用集群，凭证交给专用密钥系统，策略缓存需要版本号和失效机制，审计流水应与业务参数分离存储并进行敏感字段脱敏。
